import React from 'react';
import ArticlesList from '../../../COMMON/list/ArticlesList';

function BestsArticles({user}) {
    return (
        <>
           <ArticlesList note={3.5}/>   
        </>
    );
}

export default BestsArticles;